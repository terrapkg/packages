# Generated by rust2rpm 27
%bcond check 1

%global pokesprite pokesprite

%global crate pokeget

Name:           rust-pokeget
Version:        1.6.4
Release:        %autorelease
Summary:        Display pokemon sprites in your terminal

License:        MIT
URL:            https://crates.io/crates/pokeget
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  anda-srpm-macros
BuildRequires:  mold
Provides:       pokeget = %{version}-%{release}
Provides:       bundled(%pokesprite) = %{pcommit}
Packager:       Gilver E. <rockgrub@disroot.org, madonuko <mado@fyralabs.com>

%global _description %{expand:
Display pokemon sprites in your terminal.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
License:        (0BSD OR MIT OR Apache-2.0) AND (Apache-2.0 OR BSL-1.0) AND (Apache-2.0 OR MIT) AND (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND BSD-2-Clause AND (BSD-2-Clause OR Apache-2.0 OR MIT) AND BSD-3-Clause AND MIT AND (MIT OR Apache-2.0) AND (MIT OR Apache-2.0 OR Zlib) AND (MIT OR Zlib OR Apache-2.0) AND (Unlicense OR MIT) AND (Zlib OR Apache-2.0 OR MIT)
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license data/%pokesprite/license.md
%license LICENSE.dependencies
%doc OTHER_PROJECTS.md
%doc README.md
%{_bindir}/pokeget
%{_bindir}/pokeget-rs

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%license %{crate_instdir}/data/%pokesprite/license.md
%doc %{crate_instdir}/OTHER_PROJECTS.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep_online

%build
%{cargo_license_summary_online}
%{cargo_license_online} > LICENSE.dependencies

%install
%cargo_install
ln -sf %{crate} %{buildroot}%{_bindir}/%{crate}-rs

%if %{with check}
%check
%cargo_test
%endif

%changelog
* Sat Mar 01 2025 Gilver E. <rockgrub@disroot.org>
- Initial package
